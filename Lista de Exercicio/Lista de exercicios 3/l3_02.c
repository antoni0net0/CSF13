#include <stdio.h>
#include <math.h>

int main(){

            float numero;
            printf("digite um numero: ");
            scanf("%f", &numero);

            printf("a raiz do numero digitado eh: %.2f\n", sqrt(numero));
return 0;
}
